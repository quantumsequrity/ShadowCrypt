[package]
name = "shadowcrypt-core"
version = "0.1.0"
edition = "2021"
description = "Core game logic library for ShadowCrypt roguelike"
authors = ["bloodraven"]

[dependencies]
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
flate2 = "1.0"
